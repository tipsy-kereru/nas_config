# --- General Settings ---
DOMAIN=yourdomain.com
TZ=Pacific/Auckland

# --- Authelia Settings ---
# Generate secrets using: docker run --rm authelia/authelia authelia crypto hash generate pbkdf2 --variant sha512 --salt 32 --password your_password
AUTHELIA_JWT_SECRET=your_jwt_secret_here
AUTHELIA_SESSION_SECRET=your_session_secret_here
AUTHELIA_STORAGE_ENCRYPTION_KEY=your_storage_encryption_key_here

# --- Vaultwarden Settings ---
# Set to false once the initial account is created
SIGNUPS_ALLOWED=true
ADMIN_TOKEN=your_admin_token_here

# --- CrowdSec Settings ---
COLLECTIONS=crowdsecurity/caddy crowdsecurity/authelia

# --- SMTP Settings (Optional, but recommended for Authelia/Vaultwarden) ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=user@gmail.com
# SMTP_PASSWORD=app_password
# SMTP_FROM=admin@yourdomain.com
